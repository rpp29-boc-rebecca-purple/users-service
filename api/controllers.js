const db = require('../service');
const helpers = require('./helpers');

module.exports = {
  getExample: (req, res) => {
    // Do your stuff here...
    res.send(200);
  },

  setExample: (req, res) => {
    // Do your stuff here...
    res.send(200);
  },
};
